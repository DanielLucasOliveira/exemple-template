import Layout from "@/components/template/Layout";

export default function Notifications(params: any) {

    return (
        <Layout titulo="notifications" subtitulo="Manage your notifications">
            <h1>Notifications</h1>
        </Layout>
    );
};
