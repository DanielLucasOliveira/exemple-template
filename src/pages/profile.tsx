import Layout from "@/components/template/Layout";

export default function Profile(params: any) {

    return (
        <Layout titulo="profile" subtitulo="Manage your Profile">
            <h1>Profile</h1>
        </Layout>
    );
};
